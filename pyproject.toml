[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rtt-client"
version = "0.1.0"
description = "Small command-line client for the Realtime Trains Next Generation API"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "RTT API explorer" }]
dependencies = []

[project.optional-dependencies]
snapshots = ["Pillow>=10.4,<13"]
mcp = ["mcp==1.30.0", "tzdata>=2025.1; sys_platform == 'win32'"]

[project.scripts]
rtt = "rtt_app.cli:main"
rtt-gui = "rtt_app.gui:main"
rtt-assistant = "rtt_app.assistant_gui:main"
rtt-action = "rtt_app.action_api:main"
rtt-mcp = "rtt_app.mcp_server:main"

[tool.setuptools.packages.find]
include = ["rtt_app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
